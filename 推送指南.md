# 🚀 推送到 GitHub 和 Gitee 操作指南

## 准备工作（5 分钟）

### 第 1 步：创建 SSH Key（如果没有）

```bash
ssh-keygen -t ed25519 -C "你的邮箱@example.com"
cat ~/.ssh/id_ed25519.pub
# 复制输出的内容
```

### 第 2 步：添加到 GitHub

1. 打开 https://github.com/settings/keys
2. 点 "New SSH Key"
3. 粘贴公钥，保存
4. 测试: `ssh -T git@github.com`

### 第 3 步：添加到 Gitee（码云）

1. 打开 https://gitee.com/profile/sshkeys
2. 点 "添加公钥"
3. 粘贴公钥，保存
4. 测试: `ssh -T git@gitee.com`

---

## 创建仓库并推送

### GitHub

1. 打开 https://github.com/new
2. Repository name: `shuaishuai-kids-park`  
3. Description: 帅帅亲子乐园 · 小程序全栈方案 | 改个店名就能用的儿童乐园小程序
4. 选 Public，不要勾 Initialize README
5. 点 Create repository
6. 复制仓库地址（SSH 格式），比如: `git@github.com:你的用户名/shuaishuai-kids-park.git`

### Gitee

1. 打开 https://gitee.com/projects/new
2. 仓库名称: `shuaishuai-kids-park`
3. 选 Public，去掉"使用 Readme 初始化"
4. 点创建
5. 复制仓库地址（SSH 格式）

---

## 一键推送

把下面两行里的地址替换成你的，然后粘贴到终端：

```bash
cd "/Users/panshuai/Desktop/用所选项目新建的文件夹/丹丹/帅帅亲子乐园"

# GitHub
git remote add origin git@github.com:你的用户名/shuaishuai-kids-park.git
git push -u origin main

# Gitee
git remote add gitee git@gitee.com:你的用户名/shuaishuai-kids-park.git
git push -u gitee main
```

---

## 后续更新

代码改了之后，一条命令同步两边：

```bash
git add . && git commit -m "更新说明" && git push origin main && git push gitee main
```
